{
  "$comment": "Codex CLI hook config (ADR-029). UNVERIFIED. Codex shipped hooks.json in early 2026 and the sources place it at ~/.codex/hooks.json with the same JSON-on-stdin, exit-2-blocks convention, but no Codex host has been available to this project -- see issue 37, still unrun, and issue 42. Event names below are the convention's defaults and are a best guess. Treat this file as a starting point, not a tested artifact; correct it against the Codex docs before use.",
  "version": 1,
  "hooks": {
    "beforeSubmitPrompt": [
      { "command": "python3 RG_SKILL_DIR/tools/hooks/governance-hook.py prompt" }
    ],
    "preToolUse": [
      { "command": "python3 RG_SKILL_DIR/tools/hooks/governance-hook.py write" }
    ],
    "postToolUse": [
      { "command": "python3 RG_SKILL_DIR/tools/hooks/governance-hook.py capture" }
    ]
  }
}
